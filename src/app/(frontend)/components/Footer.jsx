const Footer = ({segment}) => {
    return (
        <footer className={`py-4`}>
            <nav className="container">
                Footer
            </nav>
        </footer>
    );
}
 
export default Footer;